
export interface IntButtonProps {
	buttonInner: React.ReactNode;
	onClick: Function;
	loading?: boolean;
	inlineStyles?: Object;
	isFullHeight?: boolean;
	isTransparentBox?: boolean;
	isHalfOpacity?: boolean;
	isHidden?: boolean;
}
